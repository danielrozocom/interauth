// This script is deprecated.
// The project now uses `server.js` as a startup wrapper that performs
// environment validation and then runs `build/index.js`.
//
// Keeping this file as a no-op to avoid accidental execution by older
// tooling. Remove this file manually if you prefer it absent from the repo.

console.log("scripts/postbuild-check.js is deprecated — no action taken.");
